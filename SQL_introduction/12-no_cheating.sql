-- Write a script that updates the score of Bob to 10 in the table second_table.

-- Update a record in table by changing score value filtering by name
UPDATE second_table
SET score = 10
WHERE name = 'Bob';
